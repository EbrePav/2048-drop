
// ============ СИНХРОНИЗАЦИЯ СОСТОЯНИЯ ИГРЫ ============
// Загружаем sync-game-state.js функции выше

// При инициализации игры:
if (authToken) {
  syncWithServer().then(() => {
    startAutoSave();
  });
}

// При game over (найти функцию endGame и добавить):
// await endGameSession();

// При клике на дневную награду (найти функцию и добавить):
// await claimDailyReward(false);
